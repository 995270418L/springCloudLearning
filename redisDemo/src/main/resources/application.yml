spring:
  application:
    name: redisDemo
  redis:
    database: 0 # Redis数据库索引
    host: 127.0.0.1
    port: 6379
    password:
    pool:
      max-active: 8 #使用负值表示没有限制
      max-wait: -1  #连接池最大阻塞时间
      max-idle: 8   #连接池中最大空闲连接
      min-idle: 0
    timeout: 0   #连接超时时间
